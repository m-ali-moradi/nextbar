# Shared credentials (required)
DB_USERNAME=replace_me
DB_PASSWORD=replace_me
JWT_SECRET=replace_with_base64_or_long_random_secret
RABBITMQ_USERNAME=replace_me
RABBITMQ_PASSWORD=replace_me
INTERNAL_SERVICE_SECRET=replace_with_long_random_secret

# Drop-points local compatibility toggles
# false => use JPA update mode locally (default)
# true  => enable Flyway migrations if DB version is supported
FLYWAY_ENABLED=false
JPA_DDL_AUTO=update
